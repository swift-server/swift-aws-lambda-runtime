version: "3"

services:

  runtime-setup:
    image: swift-aws-lambda:16.04-5.1
    build:
      args:
        ubuntu_version: "xenial"
        swift_version: "5.1"

  test:
    image: swift-aws-lambda:16.04-5.1

  shell:
    image: swift-aws-lambda:16.04-5.1
